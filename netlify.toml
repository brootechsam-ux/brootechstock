[build]
  base = "stock-control-complete"
  command = "npm install && npm run build"  # ← Adicionado npm install
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  NODE_VERSION = "18"
